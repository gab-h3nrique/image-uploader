/* components */

/* components */

export interface Props {
    page: string;
    children: React.ReactNode
}


const Navbar = ({page, children}:any) => {
    return (
        <nav className={`min-w-full h-fit bg-slate-900 p-3 text-white`}>
            a
        </nav>
    )
}
export default Navbar;